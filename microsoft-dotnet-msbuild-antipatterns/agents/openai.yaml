interface:
  display_name: "microsoft-dotnet-msbuild-antipatterns"
  short_description: "MSBuild anti-pattern detection"
  default_prompt: "Use $microsoft-dotnet-msbuild-antipatterns for MSBuild anti-pattern detection."

policy:
  allow_implicit_invocation: true
