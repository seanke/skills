interface:
  display_name: "microsoft-dotnet-msbuild-troubleshooting"
  short_description: "MSBuild build failure analysis"
  default_prompt: "Use $microsoft-dotnet-msbuild-troubleshooting for MSBuild build failure analysis."

policy:
  allow_implicit_invocation: true
